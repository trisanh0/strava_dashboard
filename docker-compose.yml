services:
  strava-dashboard:
    build: .
    container_name: strava-dashboard
    restart: unless-stopped
    ports:
      - "8501:8501"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}

